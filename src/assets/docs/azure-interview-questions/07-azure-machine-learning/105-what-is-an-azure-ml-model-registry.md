# What is an Azure ML Model Registry?

## Short answer
The Azure ML Model Registry is the catalogue of registered model versions and the boundary between training and deployment.

## Key points
- Stores model artifacts, metadata, tags and lineage.
- Registries can be shared across workspaces and environments.

## CWD context
Approval status is recorded as tags or a registry field.
